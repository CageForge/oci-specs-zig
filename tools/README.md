# Schema Generation Tools

This directory will contain the Zig code that transcodes JSON schemas into strongly typed Zig modules. The initial implementation will expose a `zig build generate` step that refreshes `src/*` directly from the schema definitions in `../schemas`.

